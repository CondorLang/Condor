import "console", "fs", "array", "string"

string[] files = fs.readDir("./src", false);

for (int i = 0; i < arylen(files); i++){
	// var file = files[i];
	// if (!fs.isDir(file)){
	// 	println(file);
	// }
}

func extractComments(string contents){
	
}