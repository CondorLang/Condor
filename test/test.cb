import {
	"console",
	"string"
}

var a = new String("This is a test");
println(a.length);
string c = a.raw; // access to raw returns nothing
//println(c); // Check this error out
int len = strlen(c);
println(len);