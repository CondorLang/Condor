import "console", "string", "array"

int[] ary = [1, 2, 3, 4, 5, 6, 7];

for (var i = 0; i < arylen(ary); i++){
	bool even = ary[i] % 2 == 0;
	if (even) println(ary[i] + ": is even");
	if (!even) println(ary[i] + ": is not even");
}