#mode "strict";

include "test2.cb" as test;

func multipleParans(){
	return (((((((1))) + 1))));
}

func toInt(int a){
	return ((int) a + multipleParans());
}

func main(string args[], int argc){
	return test.add(toInt(1), 2);
}