import {
	"console",
	"string",
	"types"
}

include "tests/int.cb"

func add(var a, var b){
	return a + b;
}

string a = "12";
int b = (int) a;
println("Your age is: " + b);
println("Your half age is: " + (b / 2));
println("Type: " + type(b));

println(add(1, 10) / 2);