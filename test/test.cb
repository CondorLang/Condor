import "console", "fs", "array", "string"

string val = "Apple";

println(indexOf(val, 'l'));

var dir = fs.readDir("./", true);

for (int i = 0; i < arylen(dir); i++){
	if (fs.isDir(dir[i])){
		println(dir[i]);
	}
}

// var a = new Array([1, 2, 3, 4]);
// var b = new Array([5, 6, 7, 8]);
// b.merge(a);
// println(b.length);